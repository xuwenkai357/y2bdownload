# 🎵 音乐文件名整理工具使用说明 (Rename Guide)

本工具用于批量整理本地文件夹下的音乐或视频文件名，清除无用的后缀噪音（如 `官方歌詞版MV`、`(Official Lyrics MV)` 等），并将其统一规范化为 `音乐名 - 专辑名` 格式。

---

## 🚀 快速使用步骤

1. **打开终端 (Terminal)**  
   在 Mac 电脑上打开自带的“终端”应用（可以通过 Command + 空格键，搜索 `Terminal` 打开）。

2. **进入项目目录**  
   在终端中输入并执行以下命令：
   ```bash
   cd /Users/xdok/Documents/GitHub/y2bdownload
   ```

3. **运行脚本**  
   输入以下命令并按回车：
   ```bash
   python3 rename_music.py
   ```

4. **输入或拖入文件夹**  
   - 终端会提示：`请输入目标文件夹路径`。
   - **方式 A (推荐)**：从 Finder (访达) 中直接把您存放音乐的文件夹 **拖拽** 到终端窗口中，终端会自动生成路径，然后按回车。
   - **方式 B**：如果直接按回车，默认会整理当前目录下的文件。

5. **预览并确认**  
   - 脚本会进入 **安全预览 (Dry Run) 模式**，列出所有会被修改的文件：
     - `[1] 原文件名: 【從我興起 Revival Starts with Me】...`
     - `    新文件名: 從我興起 Revival Starts with Me - 讚美之泉敬拜讚美 (31).mp4`
   - 查看无误后，输入 `y` 并按回车，即可真正执行重命名。输入其他任意键可安全退出。

---

## 🌟 特色功能

1. **原格式保留**：绝不改变文件原本的后缀名（如 `.mp3`, `.mp4`, `.m4a` 等）。
2. **防覆盖冲突**：如果重命名后的文件名在文件夹中已存在，工具会自动在末尾加上 `(1)`、`(2)` 等数字序号，绝不会覆盖或损坏您的原文件。
3. **粘连修复**：能智能识别像 `ReasonMV` 这样英文歌名和 `MV` 粘连在一起的情况，修复为 `Reason`。

---

## 🛠️ 后续如何自行添加“过滤词”

如果您后续发现有新的词（例如 `(Official Live Video)` 或 `官方版`）需要过滤，可以直接用文本编辑器打开 [rename_music.py](file:///Users/xdok/Documents/GitHub/y2bdownload/rename_music.py)，找到最上方的 `NOISE_PATTERNS` 列表并添加：

```python
# 2. 正则噪音模式列表（不区分大小写）
NOISE_PATTERNS = [
    # 匹配括号/方括号中的噪音词
    r'\s*[\(\[\{【](?:Official\s+)?(?:Lyrics\s+MV|Lyrics|MV|Music\s+Video|Video|Audio|HD|HQ|1080P|Clip)[\)\]\}】]',
    
    # 匹配末尾的中文噪音
    r'\s*(?:官方)?(?:歌詞版MV|歌词版MV|歌詞版|歌词版|MV|视频|音频|字幕版|字幕)\s*$',
    
    # === 可以在这里添加您想要的过滤词 ===
    # 例如：过滤文件名末尾的 "官方版" 或者 "(Live 现场版)"
    # r'\s*官方版\s*$',
]
```

---

*祝您使用愉快！如有任何疑问，可随时与我沟通。*
